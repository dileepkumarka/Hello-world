import React, { Component } from 'react';

export class Listitem extends Component {

    constructor(props) {
        super(props);
        this.state = {

        };
    }

    render() {
        console.log(this.props.reactProp);
        return (
            <React.Fragment>

            </React.Fragment>
        );
    }
}
export default Listitem;